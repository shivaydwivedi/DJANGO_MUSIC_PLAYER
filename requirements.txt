# Canonical supported Sonica runtime: Python 3.12 / Django 5.2 LTS.
# Keep this file to direct dependencies only; do not pin transitive packages
# unless the application imports/configures them directly.
Django==5.2.16
django-allauth[socialaccount]==65.18.0
python-decouple==3.8
